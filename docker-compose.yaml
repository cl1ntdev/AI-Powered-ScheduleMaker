version: "3.9"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: weekly-scheduler-backend
    ports:
      - "8011:8000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

